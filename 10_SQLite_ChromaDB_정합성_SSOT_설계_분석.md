# SQLite와 ChromaDB 간 정합성 기준점(SSOT) 설계 분석

## 1. 핵심 요약 (Executive Summary)
* **결론 및 추천 방안**: **옵션 1 (Chroma 단일 SSOT + vector_ids 폐기)** 채택을 강력히 권장합니다.
* **본질적 근거**: 
  1. 벡터 임베딩은 언제든 원본 텍스트(파서 결과물)에서 연산하여 다시 찍어낼 수 있는 **'파생 데이터(Derived Data)'**입니다. 
  2. 따라서 복잡하게 두 데이터베이스(SQLite ↔ ChromaDB) 간의 레코드를 조각별로 1:1 대조하고 맞추려는 낭비적 노력(Reconcile)을 포기하고, 불일치 발생 시 상태를 초기화하여 **'재임베딩'**하는 것이 기술적 현실성과 유지보수성에 부합합니다.
  3. SQLite에서 `vector_ids`라는 거대한 JSON 배열을 지움으로써 I/O 부하를 제거하고, 코드 기반이 얇아져 AI 기반 개발 오케스트레이션(Claude Code 활용) 시 버그 표면을 최소화할 수 있습니다.

---

## 2. 문제 배경: 왜 트랜잭션 공유가 안 되고 SSOT가 필요할까요?

### 2.1. 트랜잭션(Transaction) 공유 불가라는 환경적 현실
* **전통적 단일 DB (예: SQLite 또는 PostgreSql 단독 사용)**
  * 데이터를 갱신하거나 지울 때, 오류가 발생하면 시스템이 알아서 수정 전으로 '전체 취소(Rollback)' 해줍니다. (All-or-Nothing)
* **현행 이종 DB 구조 (SQLite + ChromaDB)**
  * CorpBrain 시스템은 문서의 메타 데이터(이름, 업로드 날짜, 파싱 상태 등)는 **SQLite**에, 문단을 분할한 공간 벡터(유사도 검색용 숫자열)는 **ChromaDB**에 분리하여 보존합니다.
  * 물리적으로 서로 다른 엔진이므로, **한 쪽에 명령을 성공했으나 전기 저하, 시스템 크래시, 디스크 잠김 등으로 다른 쪽에 실패했을 때 두 DB를 동시에 기존 상태로 자동 복구해주는 매커니즘이 없습니다.**

### 2.2. 불일치(Drift)와 고아 벡터(Orphan Vector)의 발생
* 예: SQLite 장부에서 A번 파일을 지웠는데, 직후 전원이 나가 ChromaDB에서는 안 지워졌습니다. 이제 ChromaDB에는 주인이 사라진 유령 색인(**고아 벡터**)이 남아, 검색할 때 계속해서 잘못된 결과를 반환하게 됩니다.
* 이러한 불확실성을 통제하기 위해 **"둘 간의 데이터가 어긋났을 때 누구의 상태를 최종 실진실(SSOT, Single Source of Truth)로 인정할 것인가?"**에 대한 아키텍처 규칙이 필요합니다.

---

## 3. 3가지 선택지 해설 및 비유 대조

| 구분 | Option 1: Chroma 단일 SSOT (권장) | Option 2: SQLite 권위 보존 | Option 3: 이중 기록 + 주기적 스윕 |
| :--- | :--- | :--- | :--- |
| **일상 비유** | **공식 바코드가 부착된 서류 상자**<br>장부에는 책 이름만 적고, 쪽별 코드는 안 적음. 삭제 시 "A책 번호 달린 서류 다 비워"라고 한 번에 명령. | **빼곡히 수기 기록된 관리 대장**<br>장부의 빈칸에 1쪽부터 5,000쪽의 일련번호를 전부 적음. 삭제 시 대장을 일일이 읽어 대조하며 지움. | **주야간 2교대 야간 감사조 투입**<br>장부에도 다 적고 상자도 관리하며, 새벽마다 감사조를 투입해 전수 검사로 유령 파일을 치움. |
| **데이터 구조** | SQLite의 `vector_ids` 컬럼 완전 삭제.<br>청크 ID는 `<file_id>:<index>`로 계산. | SQLite의 `vector_ids`에 대용량 JSON 배열 저장 (예: 5,000개 ID). | 양쪽 모두 데이터 기록 + 별도의 정검(Reckoning/Sweep) 배치 엔진 추가. |
| **불일치 해결** | 복구 시도 안 함 ➔ 해당 파일의 `parse_status`를 초기화하여 **전체 재임베딩**. | 두 DB 모두 직접 고쳐야 하며, 여전히 크래시 구간에서 불일치 잔존. | 대조 스윕 주기 전까지는 오류 상태가 임시 잔존하며, 스윕 로직 복잡도 증가. |
| **복합 위험도** | **매우 낮음** (단순하고 복원 확정적) | **중간** (DB 비대화 및 잠금 부하) | **높음** (예기치 못한 엣지케이스 버그 표면) |

---

## 4. 선택지별 작동 메커니즘 심도 구체화

### Option 1. Chroma 단일 SSOT + vector_ids 폐기 (권장)
* **결정론적 ID (Deterministic IDs)**
  * 과거에는 청크를 생성할 때 무작위 고유 번호(UUID)를 부여해 DB 장부에 기록해야만 "내 벡터가 무엇인지" 알 수 있었습니다.
  * 옵션 1은 **`<file_id>:<chunk_index>` (예: `doc_102:0`, `doc_102:1`)** 방식으로 공식 규칙에 따라 ID를 기계적으로 계산합니다. ID의 족보가 명확하므로 굳이 SQLite에 기록해 둘 이유가 소멸합니다.
* **크래시 복구 프로토콜 (Fail-Safe Sequence)**
  1. **삭제 및 수정 시 순서 고정**: 무조건 **[ 1단계: ChromaDB 벡터 선(先)삭제 ➔ 2단계: SQLite 커밋 ]** 순서로 작동하게 합니다.
  2. 중간에 시스템이 강제 종료되더라도 Chroma에서만 벡터가 지워지고 SQLite는 취소(Rollback)되어 이전 상태를 유지합니다. 
  3. 이 경우 고아 벡터(주인 없는 오답)가 남는 대신, 단순히 문서 텍스트만 밭에 머무르므로 **`parse_status` 상태를 '대기/오류'로 전환한 후 조용히 재임베딩**을 수행하면 완강한 정합성이 복원됩니다.

### Option 2. SQLite를 SSOT로 유지 (vector_ids 권위)
* **구조적 문제점 (SQLite Bloat & Lock)**
  * 페이지 수가 500페이지에 이르는 문서는 수천 개의 벡터 청크 ID 배열이 생성됩니다.
  * 문서를 조회하거나 작은 메타 데이터를 수정할 때도 이 거대한 JSON 텍스트가 메모리에 업로드/다운로드되면서 디스크 I/O 속도가 급차감되고 SQLite의 Lock 경쟁을 자극합니다.
* **근본 문제 방기**
  * ID 목록을 안다고 해서 DB 2개를 동시에 조작하는 원자성(Atomicity)이 만들어지지 않습니다. 삭제 중간에 멈추면 결국 누락 발생 고리는 똑같이 이어집니다.

### Option 3. 이중 기록 + 주기적 정합성 스윕
* **과잉 엔지니어링 (Over-engineering) 경계**
  * 대기업 대규모 분산 클라우드 환경에서는 유효할 수 있으나, CorpBrain과 같은 민간/로컬·단일 서비스 대상 구조에서 주기적 청소원(Audit Sweeper) 모듈을 만드는 것은 비용 대비 파급효과가 낮습니다.
  * '고아 여부 판정 알고리즘'에 조금의 버그라도 개입되면 정상적인 벡터까지 날려버리거나 무한 재생성 루프에 빠지는 2차 장애 리스크가 생깁니다.

---

## 5. 회비서님을 위한 사업기획 및 의사결정 제안

* **판단 기준 1. "파생 데이터는 간직하지 말고 재생성하라" (비용 효율성)**
  * 회비서님이 사업구조를 설계하실 때, 엑셀 표(원본)와 그 표로 만들어낸 원형 차트(파생물)가 있을 때, 차트가 약간 손상되었다고 해서 조각조각 테이프로 붙이는(Reconcile) 낭비를 하지 않습니다. 그냥 원본 표 버튼을 눌러 새 차트를 만드는 것(Re-embed)이 압도적으로 직관적이고 저렴합니다.
* **판단 기준 2. "오케스트레이션 민감성 축소" (개발 속도 유지)**
  * 향후 AI 동료(Claude 등)로 유지보수할 때 로직이 단선적이고 자명할수록(Self-explanatory) 코딩 인지 오류와 할루시네이션이 적어집니다. `vector_ids` 관리 코드를 삭제하는 것은 아키텍처 다이어트에 막대한 공헌을 합니다.
* **결정 제안**:
  * 스펙 및 문서 전반에 **[Option 1: Chroma 단일 SSOT 및 결정론적 Chunk ID 적용]**을 표준 개발 정책으로 지정을 제안드립니다.
