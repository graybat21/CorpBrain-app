# CorpBrain Grill Ledger

착수 전 미해소 결정 토픽의 단일 진척 원장. 세션이 끊겨도 첫 UNRESOLVED 토픽부터 재개한다.

## Scope Lock

| 항목 | 값 |
|:---|:---|
| **A. 참조 범위** | `docs/10_CorpBrain_PRD_v1.0.md`, `docs/SRS-draft_v0.6_OPUS.md`, `tasks/*.md` (68개) |
| **B. 관심 방향** | ① 데이터 스키마·흐름·저장 방침 ② IPC/API 계약 + 기술스택 확정 ③ LLM 라우팅·PII·보안 정책 |
| **C. 완료 조건** | 위 3개 방향의 미해소 토픽 전부 RESOLVED |
| **D. OUTPUT 대상** | 설계문서(PRD / SRS / tasks 카드) + 에이전트 하네스(`.claude/CLAUDE.md`, skills, agents, commands, hooks, settings) |
| 세션 시작 | 2026-08-06 |

## Counter

```
RESOLVED: 17 / TOTAL: 17   (CORE 12 / MINOR 5)   — ALL_RESOLVED
```

## Topics

### 방향 ② — IPC/API 계약 + 기술스택

- [x] T1 | CORE | 데스크톱 셸 및 단일 `.exe` 패키징 구조 확정 (React UI ↔ Python Core 결합 방식) | status:RESOLVED | decision:pywebview(WebView2) 단일 프로세스 + PyInstaller `--onefile`, React는 정적 SPA 번들·HashRouter로 내장. Electron/Tauri/Next.js/Node 런타임 배제 | applied:SRS §3.2 DEC-01 신설, PRD §8, tasks/APP-UI-01.md, .claude/CLAUDE.md §1·§6
- [x] T2 | CORE | Python API 프레임워크 + 로컬 서버 바인딩(포트·127.0.0.1·인증 토큰) 확정 | status:RESOLVED | decision:FastAPI+uvicorn(데몬 스레드)+Pydantic v2, `127.0.0.1` 랜덤 포트(`port=0`), 부팅 시 `secrets.token_urlsafe(32)` Bearer 토큰 필수, CORS 와일드카드 금지, OpenAPI 3.1을 계약 SSOT로 확정 | applied:SRS §3.3 DEC-02 신설, tasks/API-001.md, tasks/MOCK-001.md(Mock을 MSW로 확정), .claude/CLAUDE.md §4(+승인 의존성 목록)
- [x] T3 | CORE | IPC 필드 네이밍 컨벤션 통일 + 공통 에러 응답 스키마 (SRS §6.1 snake_case ↔ API-002 CamelCase 모순) | status:RESOLVED | decision:전 계층 `snake_case` 통일(변환 계층 없음), 경로는 단수형, 공통 봉투 `{ok,data}`/`{ok,error{code,message,field,details}}`, 표준 에러 코드 9종 확정, 부분 실패는 HTTP 207+`ok:true` | applied:SRS §6.1 DEC-03 신설, tasks/API-002.md(CamelCase 제약 교정), .claude/CLAUDE.md §4
- [x] T4 | CORE | 장기 작업 비동기 실행 모델 및 진행률 전달 방식 (`task_id` 생성·수명·재시작 복구, polling ↔ websocket 모순) | status:RESOLVED | decision:202+`task_id` 즉시 반환 + 1초 폴링. WebSocket/SSE 금지. task 상태를 신규 `Async_Task` 테이블에 영속화(파일 단위 커밋), 부팅 시 좌초 task를 `interrupted`로 전이하고 자동 재개 금지. 상태 6종·7일 보관 확정 | applied:SRS §6.1 DEC-04 신설 + §6.2.8 Async_Task 테이블·ERD 갱신(7→8개), tasks/DB-001.md(8테이블+복구 AC), tasks/API-002.md(Task DTO), tasks/LLM-CMD-03.md(웹소켓 모순 제거), .claude/CLAUDE.md §4

### 방향 ① — 데이터 스키마·저장 방침

- [x] T5 | CORE | DB 접근 계층·ORM·마이그레이션 도구 확정 (DB-001 "Alembic 또는 Prisma" 모순, CLAUDE.md는 `sqlite3` 표준 라이브러리 지향) | status:RESOLVED | decision:표준 `sqlite3`만 사용(SQLAlchemy/SQLModel/Alembic/Prisma 배제). 테이블별 얇은 Repository에 SQL 격리, Row→Pydantic 명시 변환. `PRAGMA user_version` 순차 마이그레이션(버전당 단일 트랜잭션), 스레드-로컬 커넥션, 커넥션마다 WAL·foreign_keys=ON·busy_timeout=5000·synchronous=NORMAL | applied:SRS §6.2 DEC-05 신설, tasks/DB-001.md(도구·커넥션·PRAGMA 전면 교정), .claude/CLAUDE.md §1·§4·§6
- [x] T6 | CORE | 벡터 DB 및 임베딩 모델 확정 (ChromaDB ↔ FAISS 병기, 임베딩 모델·차원수 미정, 오프라인 번들 가능성) | status:RESOLVED | decision:ChromaDB PersistentClient 확정(FAISS 배제), 워크스페이스당 컬렉션 `ws_<id>`·cosine. 임베딩은 Ollama `nomic-embed-text` 768차원을 명시 주입(Chroma 기본 ONNX 임베딩 금지), torch/sentence-transformers 번들 금지. 모델 변경 시 App_Config 가드 + 사용자 동의 후 전체 재임베딩 | applied:SRS §6.2 DEC-06 신설, PRD §8, tasks/DB-002.md(전면 구체화+AC 2건 추가), .claude/CLAUDE.md §4(+금지 의존성 목록)
  - **파급:** Option A(클라우드) 사용자도 심층 분석 시 Ollama 임베딩 모델(~274MB)이 필요 → T13 온보딩 UX에서 "임베딩 전용" vs "생성 모델" 구분 필수
- [x] T7 | CORE | `Analytics_Log` 스키마 결손 보정 (§6.3.4의 압축률·딥링크 집계에 필요한 `file_id`/`wiki_id` 컬럼 부재 → 산출 불가) | status:RESOLVED | decision:혼합안 — `Analytics_Log`에 `file_id`/`wiki_id` nullable FK(ON DELETE SET NULL) + `(created_at)`·`(event_type,created_at)` 인덱스 추가. 압축률은 이벤트 누적이 아닌 `File_Meta`/`Wiki_Content` 스냅샷 COUNT로 산출(기간 필터 미적용, `knowledge_ratio_scope:'current'` 명시). 나머지 3지표는 기간 필터 지원 | applied:SRS §6.2.5 컬럼·인덱스 추가 + DEC-07 신설 + ERD·§6.3.4 시퀀스·§6.1 응답 정정, tasks/STAT-CMD-01.md(AC 2건·event_type 표기 통일), tasks/STAT-QRY-01.md(압축률 정의 모순 교정)
- [x] T8 | CORE | Rename ↔ `File_Meta.original_path` / `Wiki_Content.deeplink_mappings` 경로 정합성 정책 (rename 후 딥링크 stale, REQ-FUNC-022와 충돌) | status:RESOLVED | decision:ID 기반 앵커 + 경로 컬럼 분리. 위키에는 `[[file_id:UUID]]`만 저장하고 경로는 조회 시점 late binding(`deeplink_mappings`=문장인덱스→file_id). `File_Meta`를 `current_path`(가변)/`original_path`(불변)로 분리, 열기·존재검증은 항상 `current_path`. Rename·Undo·Watcher 이동은 단일 행 UPDATE로 흡수하고 위키 본문 불변. `FileMovedEvent`는 file_id 보존 갱신(재등록 금지). REQ-FUNC-022는 "DB에 경로 있으나 디스크에 실물 없음"으로 재정의 | applied:SRS §6.2.2 컬럼 분리 + §6.2.3 DEC-08 신설 + §6.2.4 old/new_paths 형식 + REQ-FUNC-022 재정의 + ERD·§6.3.1 시퀀스 정정, tasks/DL-CMD-01·DL-CMD-02·DL-QRY-01·RN-CMD-02·RN-CMD-03·WA-CMD-02·DB-001(AC 5건 추가), .claude/CLAUDE.md §1·§4(Deeplink Anchoring LOCKED)
- [x] T9 | CORE | SQLite ↔ Vector DB 정합성·삭제 정책 (`vector_ids` 이중 기록, 워크스페이스 삭제 시 CASCADE·고아 벡터 처리, 트랜잭션 경계) | status:RESOLVED | decision:Chroma 단일 SSOT + `File_Meta.vector_ids` 폐기. chunk ID를 `<file_id>:<chunk_index>`로 결정론적 계산(저장 안 함), 삭제는 메타데이터 필터 `where={file_id}`. 재분석은 delete→upsert 순서 강제. 쓰기 순서 고정(Chroma delete→upsert→SQLite parse_status 커밋), 삭제 순서 고정(벡터→SQLite). 고아 벡터는 검색 후처리 lazy delete로 회수(조정 스윕·GC 스케줄러 미구현). 벡터는 재생성 가능한 파생 데이터 → 불일치 해법은 재임베딩. workspace_id FK 전부 ON DELETE CASCADE, Chroma 호출은 SQLite 트랜잭션 외부 | applied:SRS §6.2 DEC-09 신설 + §6.2.2 vector_ids 폐기 + FK 6곳 CASCADE 명시 + ERD 정정, tasks/DB-002(VectorDBManager API·AC 2건 추가), tasks/DB-001(CASCADE·AC 1건), tasks/ANA-CMD-02, tasks/WA-CMD-03, tasks/WS-CMD-01(삭제 순서), .claude/CLAUDE.md §4(Cross-store consistency LOCKED)
- [x] T10 | MINOR | 테이블명 통일 (SRS §6.2.7 `App_Config` ↔ LLM-CMD-01 `Settings_Meta` 모순) | status:RESOLVED | decision:`App_Config` 단일안 확정, `Settings_Meta` 폐기. `*_Meta`는 엔티티 메타데이터 전용 접미사이므로 전역 KV 설정에는 부적합. 목적별 설정 테이블 분리 금지 — 키 네임스페이스(`llm_*`, `embedding_*`)로 구분 | applied:SRS §6.2.7 DEC-10 신설 + 키 예시 보강, tasks/LLM-CMD-01.md(`Settings_Meta`→`App_Config`, key='llm_mode'), .claude/CLAUDE.md §4
- [x] T11 | MINOR | UUID·DATETIME 저장 타입·타임존 규약 + SQLite 미지원 `ON UPDATE CURRENT_TIMESTAMP` 대체 방식 | status:RESOLVED | decision:UUID=TEXT(하이픈 36자 소문자), DATETIME=TEXT ISO-8601 **UTC**(`...Z`, 사전순=시간순), `last_modified`만 REAL epoch 예외. 저장·전송은 전부 UTC(`datetime.now(timezone.utc)`), KST 변환은 프론트 표시 단계 한정. 기간 경계는 프론트가 로컬 기준으로 산출한 `from`/`to` UTC를 전달(백엔드가 주 경계 추정 금지). `ON UPDATE CURRENT_TIMESTAMP`(MySQL 문법·SQLite 미지원) 폐기 → Repository UPDATE 문에서 `updated_at` 명시 대입, 트리거 미사용. 기본값은 `DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))`로 통일(`DEFAULT CURRENT_TIMESTAMP` 폐기) | applied:SRS §6.2 DEC-11 신설 + 타임스탬프 컬럼 8곳 제약 일괄 정정 + §6.1 row16 from/to 파라미터, tasks/DB-001(규약 3항목+AC 1건), tasks/STAT-QRY-01(기간 경계 산출 주체 명시), .claude/CLAUDE.md §4

### 방향 ③ — LLM 라우팅·PII·보안

- [x] T12 | CORE | Option A 클라우드 프로바이더·모델 확정 + API 키 보관 방식 (`api_key_encrypted`의 암호화 키 출처 미정) | status:RESOLVED | decision:Anthropic 단일 + `claude-sonnet-5` 확정(OpenAI 동시 지원 배제, 어댑터 인터페이스만 남겨 확장 seam 보존). API 키는 **Windows DPAPI**(`CryptProtectData`, `ctypes` — 신규 의존성 0)로 암호화해 base64로 `App_Config.api_key_encrypted`에 저장, 복호화는 호출 직전 메모리에서만. 마스터키 하드코딩·머신 고정 KDF·평문 저장 전면 금지, 설정 응답은 `api_key_configured` 불리언만 노출. 복호화 실패는 키 재입력 유도(조용한 무시 금지). 모델명·단가는 `App_Config` 키로 보관하고 실측 `usage` 토큰과 곱해 비용 산출(단가 하드코딩 금지) | applied:SRS §6.3 DEC-12 신설 + §3.1·§4.x 다이어그램·§1.2 S-03/S-04·§6.2.7 설정키 목록 정정, tasks/LLM-CMD-01.md(DPAPI 4항목 + AC 2건 + 비밀 취급 제약), .claude/CLAUDE.md §4(Cloud LLM & Secrets LOCKED, 승인/금지 의존성 갱신)
- [x] T13 | CORE | Option B Ollama 모델 확정 + 폐쇄망 온보딩 모순 해소 (완전 오프라인 지향 ↔ 인스톨러·모델 인터넷 다운로드 필요) | status:RESOLVED | decision:"오프라인"을 **정상 상태(steady state)의 속성**으로 재정의해 준비 단계와 분리. 모델 2종 역할 확정 — 임베딩 `nomic-embed-text`(768d, ~274MB, **Option A 포함 전원 필수**) / 생성 `qwen2.5:7b-instruct`(~4.7GB, Option B 전용), ID는 `App_Config.local_embedding_model`·`local_generation_model`(하드코딩 금지). 프로비저닝 2모드 — `assisted`(인스톨러 도달 가능: 무인 설치+pull) / `detect_only`(**폐쇄망: 탐지만, 설치·다운로드 시도 금지**), 인스톨러 도달성 HEAD 5초 사전확인으로 자동 판정 후 `Async_Task.result_json.provision_mode` 기록. 실패는 재시도 루프 없이 `LLM_PROVISION_REQUIRED`로 즉시 종료 + 필요 모델 목록 노출, Option A 자동 폴백 금지. `POST /llm/onboard`는 `{purpose:'embedding'\|'generation'}` → 202+`task_id`(DEC-04 정합). REQ-NF-005를 "문서 내용·경로·사용 로그의 외부 전송 금지"로 정밀화하고 허용 외부 통신 2종(마스킹 청크→Anthropic, 프로비저닝 바이너리)만 명시 | applied:SRS §6.3.2 DEC-13 신설 + 온보딩 시퀀스 전면 재작성 + REQ-FUNC-010·011 재정의 + REQ-NF-005 정밀화 + `LLM_PROVISION_REQUIRED` 에러코드 추가 + §6.1 row10·row11 응답 정정 + CON-03·ASM-06·S-04·§6.2.7 키목록 갱신, PRD §5 F2·§7, tasks/LLM-CMD-03(전면 재작성+AC 3건), tasks/LLM-QRY-01(모델 준비상태 분리+AC 2건), tasks/LLM-FE-02(2모드 분기 UI+AC 2건), tasks/LLM-CMD-01(모델 키 초기화), tasks/INF-TEST-02(측정 시점·화이트리스트+AC 1건), .claude/CLAUDE.md §1·§4(Local LLM & Provisioning LOCKED, 에러코드 목록)
- [x] T14 | CORE | PII 마스킹 범위(NER 포함 여부)·마스크 토큰 형식·무결성 검증 판정 기준 (SRS `[MASKED]` ↔ LLM-CMD-02 `***-****-****` 모순) | status:RESOLVED | decision:**정규식 전용 7종**(주민번호·전화·이메일·계좌·카드·사업자번호·여권) 확정. **NER은 MVP 범위 외** — `_ner_scan()`은 인터페이스만 남긴 no-op이며 spacy·transformers 도입 금지(`DEC-06`·CON-02 위반이자, 확률적 탐지기는 REQ-FUNC-009 Fail-Safe의 통과 기준을 세울 수 없음). 토큰 형식은 **`[PII:TYPE]`** 단일안(`[PII:RRN]` 등) — `[MASKED]`와 `***-****-****` 모두 폐기(후자는 자릿수 유출). **무결성은 2조건 AND** — ⓐ결과 재스캔 매치 0건 AND ⓑ원본 매치 문자열 substring 부재(ⓐ만으로는 부분 치환 버그, ⓑ만으로는 신규 패턴 생성을 놓침). 전 경로 fail-closed(`PII_MASKING_FAILED`), 로그·에러응답·Analytics_Log에 원본 PII 금지(타입별 건수만). 중첩 매치는 넓은 범위 우선·뒤→앞 치환, 전 패턴 중첩 수량자 배제(ReDoS). 인명·기관명 미탐(RSK-03)은 은폐하지 않고 최초 전송 전 마스킹 미리보기·명시적 동의 + 설정 화면 고정 고지로 보완 | applied:SRS §6.3.3 DEC-14 신설 + 마스킹 시퀀스 4단계 재작성 + REQ-FUNC-008·009 재정의 + REQ-NF-006·RSK-03 보강 + §6.4 PIIFilter 클래스 시그니처·note 정정 + §3.6 컴포넌트 노드, PRD §5 F2, tasks/LLM-CMD-02(전면 재작성+AC 3건), tasks/LLM-TEST-01(전면 재작성+AC 3건), tasks/INF-TEST-02(토큰 형식 정정), .claude/CLAUDE.md §1·§4(PII Masking LOCKED + 금지 의존성에 spacy·transformers 추가)
- [x] T15 | CORE | 네트워크 egress 화이트리스트 및 Zero-Telemetry 강제·검증 방식 (CON-03 / REQ-NF-005를 코드 레벨에서 보장하는 수단) | status:RESOLVED | decision:**3층 방어(defense in depth)** — ①구조: 모든 outbound를 `NetworkGuard` 단일 관문으로 통과시키고 `purpose` 태그(`llm_local`/`llm_cloud`/`provisioning`) 필수화, ②정적: `NetworkGuard` 외 모듈의 `httpx`/`requests`/`socket`/`urllib.request` import를 CI 린트로 차단, ③동적: `INF-TEST-02` 패킷 캡처를 최종 회귀 검증으로 유지. 허용 목적지 3종(`127.0.0.1:11434` / `api.anthropic.com` / Ollama 배포 호스트)을 **코드 상수**로 정의(설정·환경변수에서 읽지 않음), 호스트 **exact match** 판정(부분·접미사 매칭 금지), 위반 시 `EgressBlockedError`로 요청 생성 자체를 차단하고 로그엔 호스트·purpose만. `purpose`↔목적지 쌍 불일치도 차단. **네 번째 목적지 추가는 코드 변경이 아니라 설계 결정 변경**(DEC-15 표+REQ-NF-005 동시 갱신 없으면 거부). 원격 텔레메트리/크래시 SDK 전면 금지, `socket` 런타임 몽키패치 금지(PyInstaller 재현불가 실패 유발, 2층이 동일 목적 달성). `NetworkGuard`는 **outbound 전용** — DEC-02 루프백 서버는 대상 아님 | applied:SRS §4.2 DEC-15 신설 + REQ-NF-018 신설 + REQ-NF-005 검증방법 3층 명시 + §3.6 컴포넌트(NG·PROV 노드·egress 재배선) + §6.4 NetworkGuard 클래스·관계 추가, PRD §6.1·§7, tasks/INF-CMD-03(신규 카드 — NetworkGuard+린트 구현), tasks/INF-TEST-02(3층 검증 재구성+AC 2건+TC-SEC-004), tasks/LLM-CMD-03·LLM-QRY-01·DB-002(관문 경유 명시), .claude/CLAUDE.md §1·§4(Network Egress & Zero-Telemetry LOCKED + 금지 의존성)
- [x] T16 | MINOR | LLM 호출 실패·타임아웃 폴백 정책 (Option A 실패 시 B 자동 전환 여부) + 비용 단가 테이블 출처 (REQ-NF-016) | status:RESOLVED | decision:**엔진 자동 전환 전면 금지** — A/B 선택은 성능이 아니라 **보안 결정**이므로 자동 폴백은 사용자가 승인한 전제(외부 전송 여부·품질·비용·시간)를 동의 없이 바꾸고 한 위키에 두 모델 출력을 섞는다. 전환은 설정 화면 명시 조작만. 재시도는 **일시적 오류만**(429/5xx/타임아웃) 최대 3회 지수 백오프(1s→2s→4s+지터, `retry-after` 우선), 401/400/`EgressBlockedError`/`PII_MASKING_FAILED`는 재시도 없음. 타임아웃은 `App_Config`(`llm_timeout_connect` 10s / `llm_timeout_read` 120s / embedding 30s / health 5s) — 하드코딩 금지. **부분 실패는 파일 단위 격리** — 재시도 소진 시 해당 파일만 실패로 `Async_Task`에 누적하고 작업 계속, 최종 **207+`ok:true`+`data.failed[]`**(`file_id`·`error.code`만, 원문 청크·프롬프트 금지). 200/`ok:true`로 조용히 넘기지 않는다(빠진 문서를 사용자가 모른 채 신뢰하게 됨). 전체 중단은 사전 health 실패 또는 **연속 10건 실패** 시 `LLM_UNAVAILABLE`. 실패 파일은 `parse_status != 'parsed'`로 남아 재분석이 자동 재처리 — 별도 재시도 큐 없음. **단가는 마이그레이션 시드값 + `cloud_price_updated_at` 기준일 병기 + 설정 화면 사용자 편집**, 가격표 네트워크 자동 조회 금지(`DEC-15` 네 번째 목적지가 됨), 표시는 "추정치"이며 실제 청구액으로 표기 금지. Option B 실패도 동일 규칙 + `cost_usd=0`(미측정 `NULL`과 구분) | applied:SRS §6.3 DEC-16 신설 + REQ-NF-010·REQ-NF-016 보강(TC-AVAIL-003 신설) + §6.2.7 App_Config 키 4종 추가 + §6.4 LLMRouter 클래스 시그니처·note 갱신, PRD §5 F2·§7, tasks/LLM-CMD-01(단가 시드·기준일·타임아웃 키), tasks/LLM-TEST-02(재시도 구분·백오프·207·연속실패·멱등성 테스트 + AC 2건), tasks/ANA-CMD-02(파일 단위 실패 격리 + AC 1건), tasks/LLM-FE-01(단가 편집·기준일 UI·자동조회 금지 + AC 1건), tasks/STAT-CMD-01(`cost_usd` 0/NULL 구분), .claude/CLAUDE.md §4(LLM Failure & Cost LOCKED)
- [x] T17 | MINOR | Rename 추천 프롬프트(RenameManager → LLMRouter)의 PII 마스킹 적용 여부 — 파일명 자체가 PII일 수 있음 | status:RESOLVED | decision:**`DEC-14`의 `PIIFilter`를 그대로 재사용 + 절대 경로 전면 미전송**. Rename 경로는 Option A의 **두 번째 클라우드 송신 채널**이므로 동일 게이트(`mask()` + `validate_integrity()` 2조건 + fail-closed)를 통과한다 — Rename 전용 마스킹 로직·전용 토큰 형식·전용 예외 경로·전용 HTTP 클라이언트를 만들지 않고, **"청크인지 파일명인지" 분기도 만들지 않는다(분기가 곧 우회 지점)**. 프롬프트에는 **파일명·확장자·1-depth 폴더명·뎁스 수치만** 담고 절대 경로·드라이브 문자·`C:\Users\<계정명>`·UNC 서버명을 넣지 않는다(1-depth는 `DEC-06` 청크 메타 `folder_1depth`와 동일 단위). 마스킹의 부수효과로 추천 이름이 정화되며, 응답에 `[PII:TYPE]` 토큰이 잔존하면 파일명으로 쓰지 않고 "PII 포함 — 수동 확인 필요"로 제외한다 — **역치환(un-masking) 금지**. Windows 파일명 안전성(금지문자·예약어·후행 공백/마침표·MAX_PATH) 사전 검증, 송신은 `NetworkGuard`(`purpose='llm_cloud'`) 경유. 근거: 파일명은 문서 텍스트의 부분집합이고 `홍길동_연봉계약서_2026.docx` 한 줄은 본문 한 문장보다 PII 밀도가 높다 | applied:SRS §6.3.3 DEC-17 신설 + REQ-FUNC-016 재작성 + REQ-NF-006 확장(Rename 프롬프트 포함, TC-SEC-005 신설) + §6.4 RenameManager 클래스 시그니처·note 갱신, PRD §5 F2, tasks/RN-CMD-01(전면 재작성+AC 2건), tasks/RN-FE-01(수동확인 배지·추천실패 행+AC 1건), tasks/RN-TEST-01(마스킹·절대경로·토큰잔존·파일명안전성·우회부재 테스트+AC 1건+TC-SEC-005), tasks/LLM-CMD-02(호출자 무관 공용 게이트 명시), tasks/INF-TEST-02(Option A 송신 경로 2종 순회+AC 2 확장), .claude/CLAUDE.md §4(Rename Prompts & Path Exposure LOCKED)

## Resolution Log

_(해소되는 즉시 `decision:` / `applied:` 와 함께 위 항목이 갱신되고, 아래에 상세가 누적된다.)_
